<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.footballmojo">
    <!-- io.flutter.app.FlutterApplication is an android.app.Application that
         calls FlutterMain.startInitialization(this); in its onCreate method.
         In most cases you can leave this as-is, but you if you want to provide
         additional functionality it is fine to subclass or reimplement
         FlutterApplication and put your custom class here. -->
    <application
        android:label="Football Mojo"
        android:icon="@mipmap/ic_launcher">
        <!-- Flutter 3.41: Impeller is opt-in on Android (default on iOS).
             Reduces first-frame jank from shader compilation and smooths
             complex animations (lineup pitch paint, card flip, score roll). -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="true"/>
        <!-- Vulkan backend on devices that support it; falls back to OpenGL ES. -->
        <meta-data
            android:name="io.flutter.embedding.android.ImpellerBackend"
            android:value="vulkan"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <!-- Displays an Android View that continues showing the launch screen
                 Drawable until Flutter paints its first frame, then this splash
                 screen fades out. A splash screen is useful to avoid any visual
                 gap between the end of Android's launch screen and the painting of
                 Flutter's first frame. -->
            <meta-data
                android:name="io.flutter.embedding.android.SplashScreenDrawable"
                android:resource="@drawable/launch_background"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action
                    android:name="FLUTTER_NOTIFICATION_CLICK"/>
                <category
                    android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            <!-- ChottuLink deep links — opens footballmojo.chottu.link URLs in the app -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="footballmojo.chottu.link"/>
            </intent-filter>
            <meta-data
                android:name="com.google.firebase.messaging.default_notification_channel_id"
                android:value="@string/default_notification_channel_id"/>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
        <!-- AdMob App ID. Filled per build type via manifestPlaceholders in
             android/app/build.gradle: debug=test ID, release=real production ID. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}"/>
        <!-- Manifest-merger conflict resolution:
             google_mobile_ads declares @xml/gma_ad_services_config
             firebase_analytics  declares @xml/ga_ad_services_config
             Both claim the same Ad Services config property. AdMob's version is
             the superset (it includes the analytics topics), so we pick it. -->
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource"/>
    </application>
    <uses-permission
        android:name="android.permission.INTERNET"/>
    <!-- gal: saving share graphics to the gallery. On API 29+ scoped
         storage means no runtime permission is needed; this is only for
         API ≤ 28, hence maxSdkVersion. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <!-- AdMob uses the Advertising ID. Declared explicitly (the
         google_mobile_ads plugin also merges this in) so it's visible for
         Play review; the Data Safety form must declare Advertising ID use. -->
    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"/>
</manifest>