# App screenshots

Drop **5 portrait phone screenshots** here, named exactly:

| Filename        | Screen to capture                                  |
|-----------------|-----------------------------------------------------|
| `home.png`      | Home / live scores (the hero shot)                  |
| `bracket.png`   | World Cup 2026 bracket / knockout tree              |
| `fantasy.png`   | Fantasy XI builder (the pitch with picks)           |
| `cards.png`     | Card collection / a card detail                     |
| `news.png`      | News feed or a match detail screen                  |

**Specs**

- Format: PNG (JPG/WebP also fine — keep the same base name, update `.png` refs only if you change format).
- Orientation: portrait, aspect ratio ~9:19.5 (a normal phone screen).
- Suggested size: 1080 × 2340 px (anything close works — they're rendered inside a device frame with `object-cover`).
- No status bar / device chrome needed — the site draws the phone frame.

Until these files exist the frames render empty (the build still succeeds); add them and redeploy.
